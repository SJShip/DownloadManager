<UserControl x:Class="DownloadManager.Controls.TaskTracker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DownloadManager.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="400">
	<DockPanel>
		<DockPanel.ToolTip>
			<Label Content="{Binding Tooltip}"></Label>
		</DockPanel.ToolTip>
		<Border BorderThickness="1"></Border>
		<Image Width="25" Height="25" Source="{Binding ImageSource}"></Image>
		<Label Width="Auto" Content="{Binding Title}"></Label> 
	</DockPanel>
</UserControl>
